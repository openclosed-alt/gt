# gather / transform hub seed

Minimal Vercel-ready React/Vite seed.

## Edit links
Open `src/main.jsx` and edit the `LINKS` object near the top.

## Run locally
```bash
npm install
npm run dev
```

## Deploy
Push this repository to GitHub, then import it in Vercel.
